from django.conf.urls import url
from hotels.views import *

urlpatterns = [
    # url(r'^$', HotelView.as_view(), name='index'),
    # url(r'^detail/(?P<pk>[0-9]+)/$', HotelDetailView.as_view(), name='detail'),
    # url(r'^form/$', GenerateRandomUserView.as_view(), name='user_from'),
]
